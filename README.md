<<<<<<< HEAD
# Barbarita
Este repositorio va a ser el primero que creo y la idea es poder aprender más sobre GitHub a partir de su creación
=======
# Barbara Aceto — Portfolio (Vite + React + TypeScript)

Development & deploy

Install dependencies:

```bash
npm install
```

Run dev server:

```bash
npm run dev
```

Build:

```bash
npm run build
```

Preview production build:

```bash
npm run preview
```

Deploy: push repo to Vercel. `vercel.json` includes SPA rewrite rules.
>>>>>>> e8d2682 (chore: scaffold portfolio — add Vite React TypeScript app, components, styles, and mock data)
